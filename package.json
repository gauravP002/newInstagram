
{
  "name": "instagram-clone-deployment",
  "version": "1.0.0",
  "main": "server.js",
  "dependencies": {
    "express": "^4.18.2",
    "pg": "^8.11.3",
    "cors": "^2.8.5",
    "esbuild": "^0.20.1",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "build": "mkdir -p dist && esbuild index.tsx --bundle --minify --outfile=dist/bundle.js --format=iife --global-name=App --loader:.tsx=tsx --loader:.ts=ts --define:process.env.NODE_ENV=\\\"production\\\""
  }
}
